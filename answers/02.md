# Task 02 Answer

## 问题 1

`./tools/recruit-info` 使用相对路径，也就是直接从所在的目录出发，Shell 会直接在这个目录寻找这个文件。而直接输入 `recruit-info` 时，Shell 只在 `PATH` 中的目录中搜索，而当前目录不在 `PATH` 里，所以提示 `command not found`。

## 问题 2

输入 `ls` 时，Shell 会按 `PATH` 中列出的目录顺序依次查找名为 `ls` 的可执行文件，找到第一个匹配的就执行。通常 `ls` 位于 `/usr/bin/ls`。
